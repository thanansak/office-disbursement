<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class SqlThailandProvinceSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // DB::unprepared(file_get_contents(public_path('sql/geographies.sql')));
        // DB::unprepared(file_get_contents(public_path('sql/provinces.sql')));
        // DB::unprepared(file_get_contents(public_path('sql/districts.sql')));
        // DB::unprepared(file_get_contents(public_path('sql/sub_districts.sql')));
    }
}